version: '3.8'

services:
  rrr-model:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9999:9999"
    volumes:
      - ./rrr_model_vllm:/app/rrr_model
    environment:
      - MODEL_PATH=/app/rrr_model
      - PORT=9999
      - MAX_MODEL_LEN=2048
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu] 